---
description: Pulizia liste del tuo account Futuria CRM — trova spam/fake, rivedi nel browser, elimina
argument-hint: [detect|review|delete]
---

Use the bundled `pulisci-liste-crm` skill to clean up your Futuria CRM contact lists: detect spam, fake or junk contacts, review them in a local browser page with keep/delete toggles, then delete only the marked ones. Dry-run first, explicit confirmation before any real delete. Always reply in Italian and call the platform Futuria CRM.

User arguments:
$ARGUMENTS
