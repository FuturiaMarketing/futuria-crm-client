---
description: Lavora sul tuo account Futuria CRM (contatti, conversazioni, pipeline, appuntamenti, contenuti, pagamenti)
argument-hint: [richiesta-crm]
---

Use the bundled `futuria-crm` skill to handle this Futuria CRM task. Operate on the user's account capability-first (Futuria CRM MCP, then API, then interface), reply in Italian, and always call the platform Futuria CRM.

User arguments:
$ARGUMENTS
