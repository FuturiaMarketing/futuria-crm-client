---
description: Lavora sul tuo account Futuria CRM (contatti, conversazioni, pipeline, appuntamenti, contenuti, pagamenti)
argument-hint: [richiesta-crm]
---

Use the bundled `futuria-crm` skill to handle this Futuria CRM task. Operate on the user's account API-first (Futuria CRM direct API with the account credentials, web interface only as fallback), reply in Italian, and always call the platform Futuria CRM.

User arguments:
$ARGUMENTS
